const initialState = {
  friends:[
    {id: 1, name: 'Aaaa'},
    {id: 2, name: 'Bbb'},
    {id: 3, name: 'Ccc'}
  ],
}

const sidebarReducer = (state = initialState, action) => {

  return state;
}

export default sidebarReducer;